@model IEnumerable<PROYECTOMVC.Models.VENTA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MEDIO_PAGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCTO.NOM_PRODUCTO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MEDIO_PAGO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCTO.NOM_PRODUCTO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NUM_VENTA }) |
            @Html.ActionLink("Details", "Details", new { id=item.NUM_VENTA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NUM_VENTA })
        </td>
    </tr>
}

</table>
