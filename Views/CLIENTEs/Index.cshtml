@model IEnumerable<PROYECTOMVC.Models.CLIENTE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOM_CLIENTE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DIR_CLIENTE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEL_CLIENTE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_CLIENTE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIR_CLIENTE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEL_CLIENTE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DNI_CLIENTE }) |
            @Html.ActionLink("Details", "Details", new { id=item.DNI_CLIENTE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DNI_CLIENTE })
        </td>
    </tr>
}

</table>
